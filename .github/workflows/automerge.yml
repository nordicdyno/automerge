name: Merge pull requests
on: [pull_request]
jobs:
  merge:
    name: Merge
    runs-on: ubuntu-latest
    steps:
      - name: "Auto merge code passed all checks"
        uses: "pascalgn/automerge-action@v0.2.4"
        env:
          GITHUB_TOKEN: "${{ secrets.AUTOMERGE_GITHUB_TOKEN }}"
